# GROK-20260913 — hiai-opencode

Date: 2026-09-13. Project: `/mnt/data/projects/hiai-opencode`. Work in place. Version remains **0.6.6**. No commit, push, tag, release, or publish.

Coordinator accepts. This file is review evidence, not production or visual certification.

## Tasks

### HIAI-OPENCODE-T01 — orchestration gates and doctor diagnostics

Status: **review**.

Existing plan/worker/review walls were verified, not rebuilt:

- `getTaskPermissions()`: Bob → manager+workers; Manager → workers+phase-close Critic, never Plan/Bob/Manager; Plan → explore; Critic → vision+explore; Build → explore; other workers are leaves.
- Plan writes stay path-scoped to `.bob/plans` and `.bob/drafts`.
- `src/permissions.test.ts` and `src/hooks/plan-lifecycle-gate.test.ts` still encode freeze, `INVALIDATE_PLAN`, early Critic deny, and delivery Critic.

Doctor hard-fail completion:

- CLI `parseConfig` no longer swallows invalid JSON (silent `{}` hid parse failures).
- Unparseable `bob.json` is a hard fail and skips remaining doctor checks (avoids 60s MCP probes on empty config).
- Tests in `assets/cli/hiai-opencode.test.ts`:
  - exit 1: missing core models, unparseable `bob.json`, `subagent_depth=0`, stale managed `.mcp.json`
  - exit 0: complete 10-slot models; missing Lightpanda is info, not fail

### HIAI-OPENCODE-T02 — DEV-01 Lightpanda vs upstream Chrome

Status: **review**.

Workstation operator docs no longer provision Chrome:

- `AGENTS.md` bootstrap: `bun add -g agent-browser` + official Lightpanda installer. Does **not** run `agent-browser install`.
- Unsupported Lightpanda surfaces recorded (headed mode, extensions, profiles, persistent auth, `file://`, Chrome DevTools MCP / GPU flags, screenshot fidelity, Playwright/Puppeteer).
- README / `bob.env.example` still document `agent-browser install` as an **upstream-host** Chrome option.
- `scripts/check-docs.ts` fails if `AGENTS.md` contains the combined Chrome install command, if README drops `agent-browser install`, if README hard-codes `986 tests`, or if README does not point at `ROADMAP.md`.
- Vision skill CDP path prefers Lightpanda; Chrome CDP is labeled upstream-only.

No Chrome was installed. Lightpanda visual APIs remain unsupported. A `test:e2e` placeholder is not browser coverage. Source-only checks are not runtime or visual verification.

### HIAI-OPENCODE-T03 — docs/bundle/type/test gates and versions

Status: **review**.

- README: dropped stale `986 tests`; Roadmap points at `ROADMAP.md`.
- `.github/workflows/ci.yml` and `release.yml`: Bun `1.4.x` (approved DEV-01 is Bun 1.4.0). `package.json` engines remain `bun: >=1.3.0` (no blanket upgrade).
- Version **0.6.6**. No publish.

## Exact changes

| Path | Change |
|------|--------|
| `assets/cli/hiai-opencode.mjs` | Throw on invalid JSONC; fail-fast doctor on `bob.json` parse fail |
| `assets/cli/hiai-opencode.test.ts` | Hard-fail and healthy-exit doctor tests |
| `scripts/check-docs.ts` | Workstation vs upstream browser + stale README gates |
| `AGENTS.md` | Lightpanda-only bootstrap; unsupported API table |
| `README.md` | Chrome as upstream option; no test count; Roadmap link |
| `ARCHITECTURE.md` | Workstation vs upstream engine; unsupported surfaces |
| `bob.env.example` | Do not run `agent-browser install` on this workstation |
| `src/agents/vision.ts` | Screenshot failure stays on Lightpanda |
| `src/tools/agent-browser/index.ts` | Headless/screenshot hints do not tell agents to install Chrome |
| `skills/vision/SKILL.md` | Lightpanda CDP default; Chrome labeled upstream |
| `skills/vision/browser-testing-with-devtools/SKILL.md` | No workstation Chrome install |
| `.github/workflows/ci.yml` | Bun `1.4.x` |
| `.github/workflows/release.yml` | Bun `1.4.x` |
| `CHANGELOG.md` | Unreleased notes, no version bump |
| `ROADMAP.md` | Reconciled next items 1–3 |
| `TEAM_BACKLOG.md` | Tasks marked review with evidence |

Pre-existing untracked `ROADMAP.md` / `TEAM_BACKLOG.md` were not reverted.

## Checks

Helper `python3 /mnt/data/work/grok-portfolio-20260913/inplace/run-check.py bun ...` failed: `PermissionError: [Errno 13] Permission denied: '.../inplace/check-slots'`. Checks below ran with `/home/vlgalib/.bun/bin/bun` (1.4.0) directly. That is an environment blocker, not a product bug.

| Command | Exit | Summary |
|---------|------|---------|
| `python3 .../run-check.py bun run check:docs` | 1 | Helper cannot mkdir `check-slots` (EACCES) |
| `python3 .../run-check.py bun run typecheck` | 1 | same helper blocker |
| `python3 .../run-check.py bun test` | 1 | same helper blocker |
| `python3 .../run-check.py bun run ci` | 1 | same helper blocker |
| `python3 .../run-check.py bun run check:bundle-size` | 1 | same helper blocker |
| `bun run check:docs` | 0 | `check:docs PASSED` |
| `bun run typecheck` | 0 | `tsc --noEmit` clean |
| `bun test` (after doctor parse-fail fix) | 0 | **1102 pass / 0 fail**, 2110 expects, 81 files, ~7.18s |
| `bun run ci` | 0 | Biome `Checked 183 files`. No fixes applied |
| `bun run build` | 0 | 276 modules; `dist/index.js`; Dream/Distill prompts present |
| `bun run check:bundle-size` | 0 | 897.3 KB (limit 1 MB). `stat` size 918879 bytes |
| `bun --version` | 0 | `1.4.0` |
| `npm pack --dry-run` | — | **Not run.** Previous session hit a permission request; this session did not repeat it |

An earlier `bun test` before the doctor fail-fast fix: exit 1, 1101 pass / 1 fail (`exits 1 when bob.json is unparseable` timed out at 5000ms). Fixed by skipping remaining doctor checks on parse fail; re-run passed.

## Limitations

- No runtime OpenCode session, no live `hiai-opencode doctor` against a real host install.
- No visual/browser verification. Lightpanda does not cover headed Chrome APIs. `test:e2e` placeholder is not browser coverage.
- No GitHub Actions re-run after switching CI to Bun 1.4.x.
- `npm pack --dry-run` / `pack:check` not executed.
- Slot helper unusable (EACCES on `check-slots`).
- No production, DNS, DB, publish, or secret access.

## Remaining

- Coordinator: accept, then GitHub as the owner requested. Do not publish 0.6.6 again without a user-facing release.
- Optional: `npm pack --dry-run` once permitted.
- Live CI on Bun 1.4.x.
- ROADMAP item 4: OpenCode host API drift tracking.
- Runtime doctor on a real OpenCode install.
- Visual verification if/when Lightpanda is used for a UI task (not claimed here).
